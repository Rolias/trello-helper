export enum OptionsBodyEnum {
  options = 'options',
  body = 'body'
}
export enum RestCommands {
  delete = 'delete',
  get = 'get',
  post = 'post',
  put = 'put'
}
export enum CustomFieldType {
  list = 'list',
  text = 'text',
  number = 'number',
  date = 'date',
  checkbox = 'checked'
}
